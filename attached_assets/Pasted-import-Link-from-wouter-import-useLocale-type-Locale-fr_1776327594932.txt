import { Link } from "wouter";
import { useLocale, type Locale } from "../context/LocaleContext";
import { useAuth } from "../context/AuthContext";
import { ArrowRightIcon, CheckIcon } from "lucide-react";

function LocaleToggle() {
  const { locale, setLocale } = useLocale();
  return (
    <div className="flex gap-1 rounded-full border border-border bg-card/70 p-1 backdrop-blur">
      {(["ja", "en"] as Locale[]).map((l) => (
        <button
          key={l}
          onClick={() => setLocale(l)}
          className={`px-3 py-1.5 text-xs rounded-full transition-all ${
            locale === l
              ? "bg-foreground text-background"
              : "text-muted-foreground hover:text-foreground hover:bg-accent"
          }`}
        >
          {l === "ja" ? "日本語" : "EN"}
        </button>
      ))}
    </div>
  );
}

function SectionTitle({ children }: { children: React.ReactNode }) {
  return (
    <h2 className="text-2xl sm:text-3xl font-semibold tracking-tight text-foreground">
      {children}
    </h2>
  );
}

export default function LandingPage() {
  const { t } = useLocale();
  const { user } = useAuth();

  const cards = [
    { title: t.landingCard1Title, body: t.landingCard1Body },
    { title: t.landingCard2Title, body: t.landingCard2Body },
    { title: t.landingCard3Title, body: t.landingCard3Body },
  ];

  const steps = [t.landingHowStep1, t.landingHowStep2, t.landingHowStep3];

  const byokItems = [
    t.landingByokItem1,
    t.landingByokItem2,
    t.landingByokItem3,
    t.landingByokItem4,
  ];

  return (
    <div className="min-h-screen bg-background text-foreground overflow-x-hidden">
      {/* soft background */}
      <div className="pointer-events-none fixed inset-0 -z-10">
        <div className="absolute left-1/2 top-[-120px] h-[360px] w-[360px] -translate-x-1/2 rounded-full bg-foreground/5 blur-3xl" />
        <div className="absolute right-[8%] top-[22%] h-[260px] w-[260px] rounded-full bg-foreground/4 blur-3xl" />
        <div className="absolute left-[10%] bottom-[12%] h-[220px] w-[220px] rounded-full bg-foreground/3 blur-3xl" />
      </div>

      {/* Nav */}
      <header className="sticky top-0 z-30 border-b border-border/60 bg-background/75 backdrop-blur-xl">
        <div className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between gap-4">
          <Link href="/">
            <button className="text-[15px] font-semibold tracking-tight text-foreground">
              Qrooma
            </button>
          </Link>

          <div className="flex items-center gap-3 shrink-0">
            <LocaleToggle />
            {user ? (
              <Link href="/rooms">
                <button className="whitespace-nowrap px-4 py-2 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                  {t.landingGoToApp}
                </button>
              </Link>
            ) : (
              <>
                <Link href="/login">
                  <button className="whitespace-nowrap hidden sm:inline-block px-3 py-2 text-sm text-muted-foreground hover:text-foreground transition-colors">
                    {t.loginBtn}
                  </button>
                </Link>
                <Link href="/signup">
                  <button className="whitespace-nowrap px-4 py-2 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                    {t.landingGetStarted}
                  </button>
                </Link>
              </>
            )}
          </div>
        </div>
      </header>

      {/* Hero */}
      <section className="max-w-6xl mx-auto px-6 pt-24 sm:pt-32 pb-20 sm:pb-28">
        <div className="max-w-4xl mx-auto text-center">
          <div className="inline-flex items-center rounded-full border border-border bg-card/70 px-3 py-1.5 text-xs text-muted-foreground backdrop-blur mb-8">
            {t.landingModesTitle}
          </div>

          <h1 className="text-5xl sm:text-6xl md:text-7xl font-semibold tracking-tight leading-[1.05] text-foreground whitespace-pre-line mb-6">
            {t.landingHero}
          </h1>

          <p className="max-w-2xl mx-auto text-base sm:text-lg text-muted-foreground leading-8 mb-10">
            {t.landingSubcopy}
          </p>

          <div className="flex items-center justify-center gap-3 flex-wrap mb-14">
            {user ? (
              <Link href="/rooms">
                <button className="inline-flex items-center gap-2 px-6 py-3 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                  {t.landingGoToApp} <ArrowRightIcon size={14} />
                </button>
              </Link>
            ) : (
              <>
                <Link href="/signup">
                  <button className="inline-flex items-center gap-2 px-6 py-3 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                    {t.landingGetStarted} <ArrowRightIcon size={14} />
                  </button>
                </Link>
                <Link href="/login">
                  <button className="px-6 py-3 text-sm font-medium text-foreground border border-border rounded-full bg-card/70 hover:bg-accent transition-colors">
                    {t.loginBtn}
                  </button>
                </Link>
              </>
            )}
          </div>
        </div>

        {/* Product preview */}
        <div className="max-w-5xl mx-auto">
          <div className="rounded-[28px] border border-border bg-card/80 backdrop-blur-xl shadow-[0_10px_40px_rgba(0,0,0,0.04)] overflow-hidden">
            <div className="border-b border-border px-5 py-3 flex items-center gap-2">
              <div className="w-2.5 h-2.5 rounded-full bg-foreground/15" />
              <div className="w-2.5 h-2.5 rounded-full bg-foreground/10" />
              <div className="w-2.5 h-2.5 rounded-full bg-foreground/10" />
              <div className="ml-3 text-xs text-muted-foreground">Qrooma</div>
            </div>

            <div className="grid md:grid-cols-[240px_1fr] min-h-[420px]">
              <div className="border-b md:border-b-0 md:border-r border-border bg-background/60 p-4">
                <div className="space-y-3">
                  <div className="h-10 rounded-2xl bg-foreground/5 border border-border" />
                  <div className="space-y-2">
                    <div className="h-16 rounded-2xl bg-foreground/4 border border-border" />
                    <div className="h-16 rounded-2xl bg-foreground/4 border border-border" />
                    <div className="h-16 rounded-2xl bg-foreground/4 border border-border" />
                  </div>
                </div>
              </div>

              <div className="p-5 sm:p-6 bg-background/70">
                <div className="flex items-center justify-between gap-3 mb-5">
                  <div>
                    <div className="h-5 w-44 rounded bg-foreground/8 mb-2" />
                    <div className="h-4 w-28 rounded bg-foreground/6" />
                  </div>
                  <div className="h-8 w-24 rounded-full bg-foreground/6 border border-border" />
                </div>

                <div className="space-y-4">
                  <div className="max-w-[78%] rounded-2xl border border-border bg-card px-4 py-3">
                    <div className="h-3.5 w-28 rounded bg-foreground/8 mb-2" />
                    <div className="space-y-2">
                      <div className="h-3 rounded bg-foreground/6" />
                      <div className="h-3 w-[92%] rounded bg-foreground/6" />
                      <div className="h-3 w-[70%] rounded bg-foreground/6" />
                    </div>
                  </div>

                  <div className="max-w-[78%] rounded-2xl border border-border bg-card px-4 py-3">
                    <div className="h-3.5 w-20 rounded bg-foreground/8 mb-2" />
                    <div className="space-y-2">
                      <div className="h-3 rounded bg-foreground/6" />
                      <div className="h-3 w-[85%] rounded bg-foreground/6" />
                    </div>
                  </div>

                  <div className="ml-auto max-w-[72%] rounded-2xl bg-foreground text-background px-4 py-3">
                    <div className="space-y-2">
                      <div className="h-3 rounded bg-background/30" />
                      <div className="h-3 w-[82%] rounded bg-background/30" />
                    </div>
                  </div>

                  <div className="mt-6 rounded-2xl border border-border bg-card p-4">
                    <div className="h-4 w-24 rounded bg-foreground/8 mb-3" />
                    <div className="space-y-2">
                      <div className="h-3 rounded bg-foreground/6" />
                      <div className="h-3 w-[92%] rounded bg-foreground/6" />
                      <div className="h-3 w-[78%] rounded bg-foreground/6" />
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <p className="mt-4 text-center text-sm text-muted-foreground">
            {t.landingHowTitle}
          </p>
        </div>
      </section>

      {/* Appeal cards */}
      <section className="max-w-6xl mx-auto px-6 pb-24 sm:pb-28">
        <div className="grid sm:grid-cols-3 gap-4">
          {cards.map((card, i) => (
            <div
              key={i}
              className="rounded-[24px] border border-border bg-card/75 backdrop-blur-sm p-7 transition-transform duration-300 hover:-translate-y-0.5"
            >
              <p className="text-base font-semibold text-foreground leading-snug mb-3">
                {card.title}
              </p>
              <p className="text-sm text-muted-foreground leading-7">
                {card.body}
              </p>
            </div>
          ))}
        </div>
      </section>

      {/* How it works */}
      <section className="border-t border-border/70">
        <div className="max-w-6xl mx-auto px-6 py-24">
          <div className="max-w-2xl mb-14">
            <SectionTitle>{t.landingHowTitle}</SectionTitle>
          </div>

          <div className="grid sm:grid-cols-3 gap-4">
            {steps.map((step, i) => (
              <div
                key={i}
                className="rounded-[24px] border border-border bg-card/70 p-6"
              >
                <div className="w-8 h-8 rounded-full border border-border bg-background flex items-center justify-center text-xs font-medium text-muted-foreground mb-4">
                  {i + 1}
                </div>
                <p className="text-sm text-foreground leading-7">{step}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Modes */}
      <section className="max-w-6xl mx-auto px-6 py-24">
        <div className="max-w-2xl mb-12">
          <SectionTitle>{t.landingModesTitle}</SectionTitle>
        </div>

        <div className="grid sm:grid-cols-2 gap-4">
          <div className="rounded-[24px] border border-border bg-card/70 p-7">
            <p className="text-sm font-medium text-foreground mb-3">
              {t.structuredDebate}
            </p>
            <p className="text-sm text-muted-foreground leading-7">
              {t.debateDesc}
            </p>
          </div>

          <div className="rounded-[24px] border border-border bg-card/70 p-7">
            <p className="text-sm font-medium text-foreground mb-3">
              {t.freeTalk}
            </p>
            <p className="text-sm text-muted-foreground leading-7">
              {t.freeTalkDesc}
            </p>
          </div>
        </div>
      </section>

      {/* BYOK */}
      <section className="border-t border-border/70">
        <div className="max-w-6xl mx-auto px-6 py-24">
          <div className="max-w-2xl">
            <SectionTitle>{t.landingByokTitle}</SectionTitle>
            <p className="mt-4 text-sm text-muted-foreground leading-7">
              {t.landingByokLead}
            </p>

            <ul className="mt-8 space-y-4">
              {byokItems.map((item, i) => (
                <li key={i} className="flex items-start gap-3">
                  <div className="mt-1 w-5 h-5 rounded-full border border-border bg-card flex items-center justify-center shrink-0">
                    <CheckIcon size={10} className="text-muted-foreground" />
                  </div>
                  <span className="text-sm text-foreground leading-7">{item}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </section>

      {/* Footer CTA */}
      <section className="border-t border-border/70">
        <div className="max-w-6xl mx-auto px-6 py-24 sm:py-28 text-center">
          <h2 className="text-3xl sm:text-4xl font-semibold tracking-tight text-foreground mb-8">
            {t.landingFooterCta}
          </h2>

          <div className="flex items-center justify-center gap-3 flex-wrap">
            {user ? (
              <Link href="/rooms">
                <button className="inline-flex items-center gap-2 px-6 py-3 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                  {t.landingGoToApp} <ArrowRightIcon size={14} />
                </button>
              </Link>
            ) : (
              <>
                <Link href="/signup">
                  <button className="inline-flex items-center gap-2 px-6 py-3 text-sm font-medium bg-foreground text-background rounded-full hover:opacity-85 transition-opacity">
                    {t.landingGetStarted} <ArrowRightIcon size={14} />
                  </button>
                </Link>
                <Link href="/login">
                  <button className="px-6 py-3 text-sm font-medium text-foreground border border-border rounded-full bg-card/70 hover:bg-accent transition-colors">
                    {t.loginBtn}
                  </button>
                </Link>
              </>
            )}
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t border-border/70">
        <div className="max-w-6xl mx-auto px-6 h-14 flex items-center justify-between text-xs text-muted-foreground">
          <span>© 2025 Qrooma</span>
          <span className="hidden sm:inline">Built for thoughtful discussion</span>
        </div>
      </footer>
    </div>
  );
}