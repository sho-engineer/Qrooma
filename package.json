{
  "name": "adjudo-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter='!@workspace/api-spec' run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
